# What is Time Zone API
Time Zone API is a simple CLI application purely created in JavaScript, it runs in Node.js environment
and its goal is to simply fetch the time zone of a given town, nothing else. As simple as it is.